Create a method in the Person class which returns how another person's age compares. Given the instances p1, p2 and p3, 
which will be initialised with the attributes name and age,
return a sentence in the following format:

class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  compareAge(otherPerson) {
    if (this.age === otherPerson.age) {
      return `${this.name} is the same age as ${otherPerson.name}.`;
    } else if (this.age > otherPerson.age) {
      return `${this.name} is older than ${otherPerson.name}.`;
    } else {
      return `${this.name} is younger than ${otherPerson.name}.`;
    }
  }
}
